# API Routes

Place Next.js route handlers here when you are ready to implement server actions or API endpoints.

Suggested future routes:

- `leads/import/route.ts` for CSV ingestion
- `leads/analyze/route.ts` for OpenAI analysis
- `leads/route.ts` for reading dashboard data
